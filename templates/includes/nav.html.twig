{# navigation pour toutes les pages #}
<nav class="navbar bg-primary navbar-expand-lg" data-bs-theme="dark">
    <div class="container-fluid">
    <a class="navbar-brand" href= "{{ path ('accueil')}}"> Esencial Terapia</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
        {# lien sans connexion #}
        <ul class="navbar-nav">
        <li class="nav-item">
        <a class="nav-link {{ app.current_route == 'accueil' ? 'active' :''}}" href= "{{ path ('accueil')}}"> Accueil</a>
        </li>
        <li class="nav-item">
        <a class="nav-link {{ app.current_route == 'oil.index' ? 'active' :''}}" href= "{{ path ('oil.index')}}"> Huiles Essentielles</a>
        </li>
        <li class="nav-item">
        <a class="nav-link {{ app.current_route == 'homeopathie.index' ? 'active' :''}}" href= "{{ path ('homeopathie.index')}}"> Homeopathie</a>      
        </li>
            {# lien pour l'admin #}
            
            {% if is_granted ('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link {{ app.current_route == 'stockRemede.index' ? 'active' :''}}" href= "{{ path ('stockRemede.index')}}"> Gestion des stocks</a>
            </li>
            {% endif %}
        
                    
            {# et toutes les options dispo uniquement avec connexion#}
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link {{ app.current_route == 'symptomes.index' ? 'active' :''}}" href= "{{ path ('symptomes.index')}}"> Symptômes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{ app.current_route == 'favoris.index' ? 'active' :''}}" href= "{{ path ('favoris.index')}}"> Favoris</a>
            </li>    
            <li class="nav-item">
            Connecté: {{app.user.email}}
            </li>
            <li class="nav-item">
                <a class="nav-link {{ app.current_route == 'app_logout' ? 'active' :''}}" href= "{{ path ('app_logout')}}">Logout</a>
            </li>
            {% else %}
            <li class="nav-item">
                Pas connecté
            </li>
            <li class="nav-item">
                <a class="nav-link {{ app.current_route == 'app_login' ? 'active' :''}}" href="{{ path ('app_login')}}">Login</a>
            </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>